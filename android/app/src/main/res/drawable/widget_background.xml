<?xml version="1.0" encoding="utf-8"?>
<!-- Matches the mockup's .widget-card: a light card, not a solid teal
     block -- see prayer_qibla_mockup Artifact, "home-mock" section. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" />
    <corners android:radius="18dp" />
    <stroke android:width="1dp" android:color="#E3D9C2" />
</shape>

# Prayer Qibla App — session start

**Read `CONSTITUTION.md` first, in full, before doing anything else.** It is the
authoritative source for how this project works (build/test gates, git workflow, local
Flutter path, settled decisions). Nothing below overrides it — this file only adds a new
tool on top of the existing process.

